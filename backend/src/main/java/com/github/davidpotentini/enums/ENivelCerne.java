package com.github.davidpotentini.enums;

/**
 * Nível CERNE de um processo (metodologia e ciclo). Espelha o tipo Postgres {@code VLD_NIVEL_CERNE}.
 * Por ora o sistema trata apenas o Nível I ({@code CERNE_1}) — o domínio de um valor só deixa isso
 * explícito e reserva espaço para os níveis seguintes.
 */
public enum ENivelCerne {
    CERNE_1
}
