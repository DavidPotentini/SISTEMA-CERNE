spring:

  application:
    name: cerne2

  datasource:
    url: jdbc:postgresql://localhost:5432/cerne
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

    #hibernate:
      #ddl-auto: update

  #flyway:
  #  schemas: public
  #  locations: classpath:db/migration/public
  #  enabled: true